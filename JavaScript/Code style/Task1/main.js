/* author Bogdan Kurchak */

function calculator() {
  function square(value) {
      return value * value;
  }

  const a = 5;
  const b = 7;
  const c = 10;

  const aSquare = square(a);
  const bSquare = square(b);
  const cSquare = square(c);
}
